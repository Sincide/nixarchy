#!/bin/bash

set -e

nixarchy-snapshot create || [ $? -eq 127 ]
nixarchy-update-git
nixarchy-migrate
nixarchy-update-system-pkgs
nixarchy-update-restart
nixarchy-restart-waybar # removes update-available icon
