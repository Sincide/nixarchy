#!/bin/bash

if [ "$(uname -r | sed 's/-arch/\.arch/')" != "$(pacman -Q linux | awk '{print $2}')" ]; then
  gum confirm "Linux kernel has been updated. Reboot?" && nixarchy-state clear re*-required && sudo reboot now

elif [ -f "$HOME/.local/state/nixarchy/reboot-required" ]; then
  gum confirm "Updates require reboot. Ready?" && nixarchy-state clear re*-required && sudo reboot now

elif [ -f "$HOME/.local/state/nixarchy/relaunch-required" ]; then
  gum confirm "Updates require Hyprland relaunch. Ready?" && nixarchy-state clear re*-required && uwsm stop
fi

for file in "$HOME"/.local/state/nixarchy/restart-*-required; do
  if [ -f "$file" ]; then
    filename=$(basename "$file")
    service=$(echo "$filename" | sed 's/restart-\(.*\)-required/\1/')
    echo "Restarting $service"
    nixarchy-state clear "$filename"
    nixarchy-restart-"$service"
  fi
done
